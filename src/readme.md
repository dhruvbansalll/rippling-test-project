# Rippling Interview - Submission Scaffold (Flask + SQLite)

## Overview
This is a minimal scaffold implementing a REST API (CRUD) over `Item` using Flask and SQLite. Use this as a base to implement the problem statement.

## Quickstart (local)

1. Create virtualenv & install:
```bash
cd src
python3 -m venv venv
. venv/bin/activate
pip install -r requirements.txt